import type { ProviderSessionStorageOptions } from '../types';
import ProviderStorage from './storage';
declare class ProviderSessionStorage<Options extends ProviderSessionStorageOptions = ProviderSessionStorageOptions> extends ProviderStorage<Options & {
    storage: Storage;
}> {
    constructor(options: Options);
}
export default ProviderSessionStorage;
