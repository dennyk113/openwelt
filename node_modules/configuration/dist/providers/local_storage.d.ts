import type { ProviderLocalStorageOptions } from '../types';
import ProviderStorage from './storage';
declare class ProviderLocalStorage<Options extends ProviderLocalStorageOptions = ProviderLocalStorageOptions> extends ProviderStorage<Options & {
    storage: Storage;
}> {
    constructor(options: Options);
}
export default ProviderLocalStorage;
