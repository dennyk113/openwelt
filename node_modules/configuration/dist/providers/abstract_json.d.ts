import type { Data, DataRaw, DataUpdate, ProviderJSONOptions } from '../types';
import ProviderAbstractFile from './abstract_file';
declare abstract class ProviderAbstractJSON<Options extends ProviderJSONOptions = ProviderJSONOptions> extends ProviderAbstractFile<Options> {
    read(): Promise<DataUpdate>;
    readSync(): DataUpdate;
    write(data: Data | DataRaw, force?: boolean): Promise<void>;
    writeSync(data: Data | DataRaw, force?: boolean): void;
}
export default ProviderAbstractJSON;
