/* MAIN */
const isEmpty = (object) => {
    for (const key in object) {
        if (!object.hasOwnProperty(key))
            continue;
        return false;
    }
    return true;
};
/* EXPORT */
export default isEmpty;
