import get from './get';
import has from './has';
import set from './set';
import del from './del';
import flat from './flat';
import unflat from './unflat';
declare const PathProp: {
    get: (object: object, path: string, fallback?: unknown) => unknown;
    has: (object: object, path: string) => boolean;
    set: <T extends object>(object: T, path: string, value: unknown) => T;
    delete: (object: object, path: string) => void;
    flat: (object: object, prefix?: string) => object;
    unflat: (object: object) => object;
};
export default PathProp;
export { get, has, set, del, del as delete, flat, unflat };
